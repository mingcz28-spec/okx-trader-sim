- Worked on `okx-trader-sim` in `C:\Users\Ziming\.openclaw\workspace\okx-trader-sim`.
- User requirement hardened: keep OKX data real and consistent, show OKX API raw responses directly, avoid frontend-only derived views pretending to be authoritative.
- Added/expanded OKX raw data display for account balance, positions, and order history, while keeping demo/live sync and connection testing.
- Implemented strategy config for RAVE-USDT-SWAP using long-only rule: buy, then sell if price falls 1% from entry or 2% from post-entry peak; later user wanted parameter optimization via backtest rather than fixed defaults.
- Built backtest support around RAVE-USDT-SWAP 1H candles, including grid search over stop-loss/trailing-drawdown parameters, summary metrics, detailed trade points, and equity curve.
- Important product direction: front and back end must share one source of truth. Backtest summaries, selected parameters, candles, and trade points were moved toward service-side state instead of frontend-only state.
- User repeatedly emphasized data truth and consistency. Future changes should prioritize backend state, raw payload preservation, and matching parameters across table, chart, and detail views.
- Added richer backtest UI concepts: sortable results, apply-params action, K-line chart, buy/sell markers, tooltip details, trade detail view, and equity curve. Latest user feedback: equity-curve axes need to be clearer, and K-line absence online is because fresh Railway instances have no backtest detail until a run happens.
- Railway deployment succeeded after browserless login flow. Deployed app URL: `https://okx-trader-sim-production.up.railway.app`.
- Key Railway fix: default Node 18 failed with Next 16, so added `nixpacks.toml` requesting Node 20.
- Caveat for future self: many settings/state are still stored only in server memory, so Railway restarts can wipe API config, strategy config, and backtest state. Persisting config/state is a recommended next step.

- Continued work on okx-trader-sim mobile-first UI and testing. Confirmed Railway deployment remained healthy and homepage accessible.
- Verified backtest API flow end-to-end on deployed app: grid backtest returned 36 results and 12 top candidates; detail backtest for selected params returned 1000 candles and 480 trade points.
- Confirmed server-side sim state sync via /api/sim includes backtest results, chart candles, trade points, and selected parameters, reinforcing backend as single source of truth.
- Noted that simple text fetch of homepage can still show pre-backtest copy even when API state already contains results, suggesting browserless/static fetch is not a reliable proxy for hydrated frontend state.
